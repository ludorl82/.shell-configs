return {
  {
    "christoomey/vim-tmux-navigator",
    config = function()
      -- Optional: You can add any plugin-specific configuration settings here
      -- By default, vim-tmux-navigator should work without extra configuration
    end
  }
}
