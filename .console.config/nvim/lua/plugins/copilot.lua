return {
  {
    "github/copilot.vim",
    config = function()
      -- You can add additional configuration for Copilot here if needed
      -- For instance, you can customize key bindings, settings, etc.
    end
  }
}
